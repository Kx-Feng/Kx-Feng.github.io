Put downloadable files here:

- `cv.pdf`                  — linked from the sidebar and /cv/
- `teaching-statement.pdf`  — linked from /teaching/
- `slides-*.pdf`, `poster-*.pdf` — linked from publications and talks

They are served at `/files/<name>`.
